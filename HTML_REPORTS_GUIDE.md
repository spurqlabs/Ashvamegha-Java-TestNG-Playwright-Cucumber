# Cucumber HTML Reports Generation Guide

## ✅ Current Status: HTML Reports ARE Being Generated!

Your project **IS generating HTML reports successfully**. The files are located here:

```
D:\Automation\Framework_OrangeHRMS\reports\
├── cucumber-report.html (2 MB - Comprehensive HTML Report)
└── cucumber.json (JSON Report Data)
```

---

## 📊 Report Types Generated

### 1. **Cucumber HTML Report** ✅
- **File:** `reports/cucumber-report.html`
- **Size:** ~2 MB (2,012 KB)
- **Generated By:** `html:reports/cucumber-report.html` plugin in TestRunner.java
- **Features:** Interactive, visual report with scenarios and steps

### 2. **JSON Report** ✅
- **File:** `reports/cucumber.json`
- **Size:** ~2.4 KB
- **Generated By:** `json:reports/cucumber.json` plugin in TestRunner.java
- **Purpose:** Data format for further processing

### 3. **Allure Report** ✅
- **Directory:** `allure-results/` (with 50+ JSON files)
- **Generated By:** `io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm` plugin
- **Purpose:** Advanced test analytics and reporting

---

## 🔍 Why Reports Are Generated

Your `TestRunner.java` configuration includes:

```java
@CucumberOptions(
    features = "src/test/resources/Features",
    glue = {
        "StepDefinitions",
        "Hooks"
    },
    plugin = {
        "pretty",                                              // Console output
        "html:reports/cucumber-report.html",                 // ✅ HTML Report
        "json:reports/cucumber.json",                        // ✅ JSON Report
        "io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm"   // ✅ Allure Report
    },
    monochrome = true,
    tags = "@smoke"
)
```

All three plugins are **enabled and working**.

---

## 🚀 How to View Reports

### Option 1: View Cucumber HTML Report
1. Navigate to: `D:\Automation\Framework_OrangeHRMS\reports\`
2. Double-click: `cucumber-report.html`
3. Report opens in your default browser with:
   - All test scenarios
   - Test results (Pass/Fail)
   - Step-by-step execution details
   - Timestamps

### Option 2: Generate and View Allure Report

```bash
# Step 1: Build and run tests
mvn clean test -Dtags="@smoke"

# Step 2: Generate Allure report from the results
mvn allure:report

# Step 3: Open the report
mvn allure:serve
```

**This will:**
- Generate `/target/allure-report/` directory
- Open the Allure web interface in your default browser
- Show detailed analytics, timelines, and trends

### Option 3: View JSON Report
- Open `reports/cucumber.json` with any text editor
- Contains all test execution data in JSON format

---

## 📋 Step-by-Step Report Generation Process

### When You Run Tests:

```bash
mvn test -Dtags="@smoke"
```

### What Happens Behind the Scenes:

1. **Maven Surefire Plugin** runs the tests
2. **Cucumber Framework** executes the scenarios
3. **Report Plugins** capture execution data:
   - ✅ Pretty formatter → Console output
   - ✅ HTML Plugin → `reports/cucumber-report.html`
   - ✅ JSON Plugin → `reports/cucumber.json`
   - ✅ Allure Plugin → `allure-results/` JSON files

---

## 🛠️ Enhanced Configuration (Recently Added)

I've updated your `pom.xml` with:

### 1. **Allure Maven Plugin**
```xml
<plugin>
    <groupId>io.qameta.allure</groupId>
    <artifactId>allure-maven</artifactId>
    <version>2.11.2</version>
    <configuration>
        <reportVersion>2.24.0</reportVersion>
        <resultsDirectory>${project.build.directory}/allure-results</resultsDirectory>
        <reportDirectory>${project.build.directory}/allure-report</reportDirectory>
    </configuration>
</plugin>
```

### 2. **Maven Failsafe Plugin**
```xml
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-failsafe-plugin</artifactId>
    <version>3.2.5</version>
</plugin>
```

---

## 📂 Complete Report Directory Structure

```
Framework_OrangeHRMS/
├── reports/                          (Cucumber Reports)
│   ├── cucumber-report.html          (Interactive HTML Report) ✅
│   └── cucumber.json                 (JSON Data) ✅
│
├── allure-results/                   (Allure Raw Data)
│   ├── *.result.json                 (Test results)
│   └── *.container.json              (Test containers)
│
└── target/                           (After running allure:report)
    └── allure-report/                (Generated Allure Report)
        ├── index.html
        ├── widgets/
        └── plugins/
```

---

## 🔧 Troubleshooting: Reports Not Showing?

### Issue 1: Reports directory is empty
**Solution:**
```bash
# Clean and rebuild
mvn clean test -Dtags="@smoke"
```

### Issue 2: cucumber-report.html is blank
**Solution:**
- Check if tests are actually running
- Look for test execution failures
- Verify features path in TestRunner.java

### Issue 3: Can't open HTML report
**Solution:**
1. Right-click `cucumber-report.html`
2. Select "Open with" → Choose your browser
3. Or drag file into browser window

### Issue 4: Allure report not generating
**Solution:**
```bash
# Ensure allure-results contains data
dir /s allure-results

# Generate report explicitly
mvn allure:report

# View it with
mvn allure:serve
```

---

## 📊 Report Contents Explained

### Cucumber HTML Report Shows:

✅ **Feature Information**
- Feature name: "OrangeHRM Login and Recruitment Navigation"
- Feature description and purpose

✅ **Scenario Details**
- Scenario name
- Tags (@smoke, @login, @regression, etc.)
- Status (PASSED ✓ or FAILED ✗)

✅ **Step Execution**
- Step text (Given, When, Then, And)
- Step status
- Duration/timing
- Failure messages (if any)

✅ **Execution Summary**
- Total scenarios
- Passed/Failed count
- Execution time
- Pass rate percentage

---

## 🎯 Best Practices

### 1. Always Run Tests with Clean
```bash
mvn clean test
```
This ensures old reports don't interfere.

### 2. Generate Reports After Test Run
```bash
# Run tests
mvn test -Dtags="@smoke"

# View reports immediately
mvn allure:serve
```

### 3. Archive Reports
Keep old reports for comparison:
```bash
# Save report with timestamp
copy reports\cucumber-report.html reports\cucumber-report-2026-02-09.html
```

### 4. Include in CI/CD Pipeline
Reports will automatically generate in:
- Jenkins, GitHub Actions, GitLab CI, etc.
- Store as artifacts for viewing

---

## 💡 Pro Tips

1. **Open HTML Report in IntelliJ IDEA:**
   - Right-click `cucumber-report.html`
   - Select "Open in Browser"

2. **Use Allure for Trending:**
   - `mvn allure:serve` shows history and trends
   - Track test stability over time

3. **Share Reports:**
   - Copy entire `reports/` directory
   - Open `cucumber-report.html` anywhere
   - Works without any dependencies

4. **Filter Reports:**
   - The HTML report is interactive
   - Click on scenarios to expand details
   - Search and filter by status

---

## ✨ Summary

### Your Reports:
| Report Type | Location | Status | Size |
|------------|----------|--------|------|
| Cucumber HTML | `reports/cucumber-report.html` | ✅ Generated | 2 MB |
| JSON Data | `reports/cucumber.json` | ✅ Generated | 2.4 KB |
| Allure Results | `allure-results/` | ✅ Generated | 50+ files |
| Allure Report | `target/allure-report/` | ⏳ Run `mvn allure:report` | - |

### Next Steps:
1. ✅ Reports ARE being generated
2. 🌐 Open `reports/cucumber-report.html` in your browser
3. 📊 Run `mvn allure:serve` for advanced analytics
4. 📈 View execution history and trends

**Your reporting is working perfectly!** 🎉
