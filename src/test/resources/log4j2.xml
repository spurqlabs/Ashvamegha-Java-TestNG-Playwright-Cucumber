<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>

        <!-- ================= CONSOLE ================= -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d{HH:mm:ss} | %-5level | %c{1} | %msg%n"/>
        </Console>

        <!-- ================= EXECUTION LOG ================= -->
        <RollingFile name="ExecutionLog"
                     fileName="logs/execution.log"
                     filePattern="logs/archive/execution-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss} | %-5level | %logger | %msg%n"/>

            <Policies>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>

        </RollingFile>

        <!-- ================= ERROR ONLY LOG ================= -->
        <RollingFile name="ErrorLog"
                     fileName="logs/error.log"
                     filePattern="logs/archive/error-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss} | %-5level | %logger | %msg%n"/>

            <Filters>
                <ThresholdFilter level="ERROR"
                                 onMatch="ACCEPT"
                                 onMismatch="DENY"/>
            </Filters>

            <Policies>
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>

        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- ROOT LOGGER (Covers ALL Modules Automatically) -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="ExecutionLog"/>
            <AppenderRef ref="ErrorLog"/>
        </Root>

    </Loggers>

</Configuration>
